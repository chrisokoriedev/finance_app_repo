name: expense_app
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+2

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons:
  cloud_firestore:
  table_calendar:
  responsive_sizer: ^3.2.0
  gap:
  animated_custom_dropdown:
  icons_launcher: ^3.1.0
  line_icons: ^2.0.3
  flutter_riverpod: ^2.5.1
  firebase_core:
  firebase_auth:
  firebase_remote_config:
  firebase_analytics:
  firebase_ai:
  local_auth:
  url_launcher:
  flutter_easyloading:
  cached_network_image: ^3.3.1
  syncfusion_flutter_charts: ^33.2.13+1
  go_router: ^17.3.0
  google_sign_in:
  intl:
  freezed_annotation: ^3.1.0
  dartz: ^0.10.1
  injectable:
  timeago: ^3.5.0
  hooks_riverpod: ^2.5.1
  flutter_hooks:
  shared_preferences:
  vibration: ^3.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  build_runner:
  freezed: ^3.2.5
  injectable_generator:

flutter:
  uses-material-design: true

icons_launcher:
  image_path: "assets/images/app_icon.png"
  adaptive_background_color: "#A9A9A9"
  platforms:
    android:
      enable: true
    ios:
      enable: true

  assets:
    - assets/
    - assets/gifs/  
    - assets/images/

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
